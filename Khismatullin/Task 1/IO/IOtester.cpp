//#include <iostream>
#include "reader.h"
#include "breader.h"
#include "writer.h"
#include "bwriter.h"

using namespace std;

int main() {
	{
	Reader<int> *in;
	//Reader<int> tmp("input.txt");
	//in[0] = tmp;
	//Writer<int> out("output.txt");
	//BinReader<int> bin[1];
	//BinReader<int> tmp2("bin.txt");
	//bin[0] = tmp2;
	//BinWriter<int> bout("bin.txt");
	//int a, b;
	//in[0](a);
	//in[0](b);
	//bout(a);
	//bout(b);
	//bin[0](a);
	//bin[0](b);
	//out(a);
	//out(b);
	}
	//remove("bin.txt");
	
	
	return 0;
}